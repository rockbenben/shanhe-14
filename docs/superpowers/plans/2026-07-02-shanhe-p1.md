# 浮生长卷 P1（串珠叙事框架＋《山河十四年》）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建成纯故事性（无数值）串珠叙事框架，并交付《山河十四年》（1931 九一八 → 1945 胜利，12 章，记者视角）完整可玩。

**Architecture:** 故事是数据（zod 校验的 TS 对象）：线性章节串（珠），章内 beat 有向无环图（分支），章末汇聚 `$end`；选择只落 flag，flag 驱动条件路由与结局分化。引擎纯函数推进（start/enter/advance/choose/resolveEnding），localStorage 存续读，React 视觉小说式 UI。

**Tech Stack:** Vite 8 + React 19 + TypeScript 6 + zod 4 + vitest 4（与 015-thousand-lives 同底盘）。

## Global Constraints

- Node ≥ 20；依赖版本对齐 015：react ^19.2.7、zod ^4.4.3、vite ^8.1.2、vitest ^4.1.9、typescript ^6.0.3、@vitejs/plugin-react ^6.0.3。
- 纯前端静态站，无后端；P1 无 AI 调用、无数值属性、无随机事件池。
- `src/engine/` 内**禁止出现具体故事 id 字面量**（沿袭 015 铁律，引擎必须故事无关）。
- git 提交：中文 conventional 风格（feat/fix/docs/test + 中文摘要），**不带 Co-Authored-By / Claude-Session 尾行**。
- 内容红线：真实历史人物一律侧写不出场；南京（第 6 章）、河南饥荒（第 10 章）以亲历者视角克制侧写，不渲染暴行场面。
- 《山河十四年》每章 `historyNote`（史实注）必填且 ≥80 字；每个 beat 的 narrative ≥100 字；每个 A 类史实锚点在动笔前用网络检索复核（各内容任务的 Step 1）。
- 每个 beat/章可留 `art` 稳定 id（沿 015 的 art+gen 体系），P1 **不产图**，UI 无图时用渐变底色兜底。

## File Structure

```
021/
├─ package.json / tsconfig.json / vite.config.ts / index.html
├─ src/
│  ├─ main.tsx / App.tsx / styles.css
│  ├─ stories/
│  │  ├─ schema.ts(.test.ts)      故事格式与结构校验（zod）
│  │  ├─ index.ts                 builtinStories 注册表
│  │  └─ shanhe/
│  │     ├─ index.ts              装配 12 章 + endings → Story
│  │     ├─ ch01.ts … ch12.ts     每章一个文件（Chapter 对象）
│  │     └─ shanhe.test.ts        内容完整性测试
│  └─ engine/
│     ├─ cond.ts(.test.ts)        flag 条件求值
│     ├─ types.ts                 ReaderState / LogEntry
│     ├─ reader.ts(.test.ts)      推进引擎（故事无关）
│     ├─ finale.ts(.test.ts)      结局判定
│     ├─ storage.ts(.test.ts)     localStorage 存续读
│     └─ fixtures.ts              引擎测试共用的 demo 故事
└─ src/ui/
   ├─ Home.tsx / ChapterCover.tsx / Reader.tsx / EndingCard.tsx / Recap.tsx
```

---

### Task 1: 工程脚手架

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/styles.css`, `.gitignore`, `src/smoke.test.ts`

**Interfaces:**
- Produces: 可 `npm run dev / build / test` 的空壳工程。

- [ ] **Step 1: 写工程文件**

`package.json`：
```json
{
  "name": "floating-life",
  "version": "0.1.0",
  "description": "浮生长卷 — 编排式叙事游戏框架：作者编排故事，引擎负责推进与记忆",
  "author": "rockbenben",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.2",
    "vitest": "^4.1.9"
  },
  "engines": { "node": ">=20" }
}
```

`tsconfig.json`：
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["vite/client"]
  },
  "include": ["src"]
}
```

`vite.config.ts`：
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({ plugins: [react()] })
```

`index.html`：
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>浮生长卷</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/main.tsx`：
```tsx
import { createRoot } from 'react-dom/client'
import App from './App'
import './styles.css'

createRoot(document.getElementById('root')!).render(<App />)
```

`src/App.tsx`（占位，Task 7 重写）：
```tsx
export default function App() {
  return <h1>浮生长卷</h1>
}
```

`src/styles.css`（占位，Task 8 充实）：
```css
:root { color-scheme: light; }
body { margin: 0; font-family: 'Noto Serif SC', serif; background: #f5f1e8; color: #2b2620; }
```

`.gitignore`：
```
node_modules
dist
```

`src/smoke.test.ts`：
```ts
import { describe, expect, it } from 'vitest'

describe('smoke', () => {
  it('测试器可运行', () => {
    expect(1 + 1).toBe(2)
  })
})
```

- [ ] **Step 2: 安装依赖并跑冒烟测试**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" install`
Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test`
Expected: 1 passed。

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" run build`
Expected: tsc 无错，vite build 产出 dist/。

- [ ] **Step 3: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add -A
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "chore: Vite+React+TS 工程脚手架，对齐 015 依赖版本"
```

---

### Task 2: flag 条件求值 `cond.ts`

**Files:**
- Create: `src/engine/cond.ts`
- Test: `src/engine/cond.test.ts`

**Interfaces:**
- Produces: `checkCond(cond: string | undefined, flags: string[]): boolean`——`&` 连接若干项、项可带 `!` 前缀（如 `'直笔 & !弃笔'`）；`undefined`/空白恒真。

- [ ] **Step 1: 写失败测试**

`src/engine/cond.test.ts`：
```ts
import { describe, expect, it } from 'vitest'
import { checkCond } from './cond'

describe('checkCond', () => {
  it('undefined 恒真', () => {
    expect(checkCond(undefined, [])).toBe(true)
  })
  it('单 flag 命中/未命中', () => {
    expect(checkCond('直笔', ['直笔'])).toBe(true)
    expect(checkCond('直笔', [])).toBe(false)
  })
  it('取反', () => {
    expect(checkCond('!弃笔', [])).toBe(true)
    expect(checkCond('!弃笔', ['弃笔'])).toBe(false)
  })
  it('与连接（含空格容错）', () => {
    expect(checkCond('直笔 & 发稿 & !弃笔', ['直笔', '发稿'])).toBe(true)
    expect(checkCond('直笔&发稿', ['直笔'])).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- cond`
Expected: FAIL（cond.ts 不存在）。

- [ ] **Step 3: 最小实现**

`src/engine/cond.ts`：
```ts
// flag 条件求值：`&` 连接若干项，项可带 `!` 前缀；undefined/空白恒真。
// 故事格式刻意只支持「与 + 非」——结局与路由按具体分化枚举，避免作者写出难验的布尔迷宫。
export function checkCond(cond: string | undefined, flags: string[]): boolean {
  if (!cond || !cond.trim()) return true
  return cond.split('&').every((raw) => {
    const t = raw.trim()
    return t.startsWith('!') ? !flags.includes(t.slice(1)) : flags.includes(t)
  })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- cond`
Expected: PASS（4 tests）。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/engine/cond.ts src/engine/cond.test.ts
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(engine): flag 条件求值——与+非的极简表达式"
```

---

### Task 3: 故事 schema 与结构校验

**Files:**
- Create: `src/stories/schema.ts`
- Test: `src/stories/schema.test.ts`

**Interfaces:**
- Consumes: `checkCond`（仅用于文档说明，本任务不调用）。
- Produces: `storySchema`（zod）与类型 `Story / Chapter / Beat / Choice / Ending / NextRef`；常量 `CHAPTER_END = '$end'`。校验不变量见 Step 1 测试。

- [ ] **Step 1: 写失败测试**

`src/stories/schema.test.ts`：
```ts
import { describe, expect, it } from 'vitest'
import { storySchema } from './schema'

const validStory = {
  id: 'demo',
  title: '示例',
  tagline: '一个用于测试的极小故事',
  chapters: [
    {
      id: 'c1', title: '第一章', entry: 'b1',
      beats: [
        {
          id: 'b1', narrative: '开场。',
          choices: [
            { text: '向左', set: ['左'], to: 'b2' },
            { text: '向右', to: 'b2' },
          ],
        },
        { id: 'b2', narrative: '路口。', next: [{ when: '左', to: 'b3' }, { to: '$end' }] },
        { id: 'b3', narrative: '左途尽头。', next: '$end' },
      ],
    },
  ],
  endings: [
    { id: 'e-left', when: '左', title: '左途', epilogue: '你走了左边。' },
    { id: 'e-def', title: '归途', epilogue: '你回了家。' },
  ],
}

const clone = () => structuredClone(validStory)

describe('storySchema', () => {
  it('合法故事通过', () => {
    expect(() => storySchema.parse(validStory)).not.toThrow()
  })
  it('beat 必须恰有 choices 或 next 之一', () => {
    const s = clone()
    delete (s.chapters[0].beats[2] as Record<string, unknown>).next
    expect(() => storySchema.parse(s)).toThrow(/choices 或 next/)
  })
  it('引用不存在的 beat 被拒', () => {
    const s = clone()
    s.chapters[0].beats[0].choices![0].to = 'ghost'
    expect(() => storySchema.parse(s)).toThrow(/ghost/)
  })
  it('条件 next 最后一项必须无 when（兜底）', () => {
    const s = clone()
    s.chapters[0].beats[1].next = [{ when: '左', to: 'b3' }]
    expect(() => storySchema.parse(s)).toThrow(/兜底/)
  })
  it('beat 图有环被拒', () => {
    const s = clone()
    s.chapters[0].beats[2].next = 'b1'
    expect(() => storySchema.parse(s)).toThrow(/环/)
  })
  it('entry 不可达的 beat 被拒', () => {
    const s = clone()
    s.chapters[0].beats.push({ id: 'orphan', narrative: '孤岛节点。', next: '$end' })
    expect(() => storySchema.parse(s)).toThrow(/不可达/)
  })
  it('最后一个结局必须无 when，其余必须有 when', () => {
    const bad1 = clone()
    bad1.endings[1].when = '左'
    expect(() => storySchema.parse(bad1)).toThrow(/兜底结局/)
    const bad2 = clone()
    delete (bad2.endings[0] as Record<string, unknown>).when
    expect(() => storySchema.parse(bad2)).toThrow(/兜底结局/)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- stories/schema`
Expected: FAIL（schema.ts 不存在）。

- [ ] **Step 3: 实现 schema**

`src/stories/schema.ts`：
```ts
import { z } from 'zod'

// 章内终点哨兵：beat 的 to/next 指向它即「本章完」。
export const CHAPTER_END = '$end'

// 条件表达式：`&` 连接、可 `!` 取反（求值见 engine/cond.ts）。
const condSchema = z.string().regex(/^\s*!?[^\s&!]+(\s*&\s*!?[^\s&!]+)*\s*$/, '条件表达式只支持 & 与 ! 组合')

// 线性去向：beat id / '$end'，或按 flag 分派的条件表（首个满足者胜出）。
const nextRefSchema = z.union([
  z.string().min(1),
  z
    .array(z.object({ when: condSchema.optional(), to: z.string().min(1) }))
    .min(1)
    .refine((arr) => arr[arr.length - 1].when === undefined, {
      message: '条件 next 的最后一项必须无 when（兜底）',
    }),
])

const choiceSchema = z.object({
  text: z.string().min(1),
  set: z.array(z.string().min(1)).optional(), // 落下的 flag（选择的全部后果——没有数值）
  to: z.string().min(1),
})

const beatSchema = z.object({
  id: z.string().min(1),
  narrative: z.string().min(1),
  art: z.string().optional(),
  gen: z.enum(['flux', 'gemini']).optional(),
  choices: z.array(choiceSchema).min(2).max(4).optional(),
  next: nextRefSchema.optional(),
})

const chapterSchema = z.object({
  id: z.string().min(1),
  title: z.string().min(1),
  epigraph: z.string().optional(), // 章引
  historyNote: z.string().optional(), // 史实注：本章的真实底本与出处线索
  art: z.string().optional(),
  entry: z.string().min(1),
  beats: z.array(beatSchema).min(1),
})

const endingSchema = z.object({
  id: z.string().min(1),
  when: condSchema.optional(),
  title: z.string().min(1),
  epilogue: z.string().min(1),
  art: z.string().optional(),
})

export const storySchema = z
  .object({
    id: z.string().min(1),
    title: z.string().min(1),
    tagline: z.string().min(1),
    cover: z.string().optional(),
    chapters: z.array(chapterSchema).min(1),
    endings: z.array(endingSchema).min(1),
  })
  .superRefine((story, ctx) => {
    story.chapters.forEach((ch, ci) => {
      const ids = new Set(ch.beats.map((b) => b.id))
      const edges = new Map<string, string[]>()
      ch.beats.forEach((b, bi) => {
        const hasChoices = !!b.choices
        const hasNext = b.next !== undefined
        if (hasChoices === hasNext) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            path: ['chapters', ci, 'beats', bi],
            message: `beat "${b.id}" 必须恰有 choices 或 next 之一`,
          })
          return
        }
        const tos = hasChoices
          ? b.choices!.map((c) => c.to)
          : typeof b.next === 'string'
            ? [b.next]
            : b.next!.map((n) => n.to)
        edges.set(b.id, tos)
        tos.forEach((to) => {
          if (to !== CHAPTER_END && !ids.has(to)) {
            ctx.addIssue({
              code: z.ZodIssueCode.custom,
              path: ['chapters', ci, 'beats', bi],
              message: `beat "${b.id}" 引用了不存在的节点 "${to}"`,
            })
          }
        })
      })
      if (!ids.has(ch.entry)) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          path: ['chapters', ci, 'entry'],
          message: `entry "${ch.entry}" 不在本章节点中`,
        })
        return
      }
      // 可达性 + 无环（DFS 三色标记）：白 0 / 灰 1 / 黑 2
      const color = new Map<string, number>()
      const visit = (id: string): void => {
        if (id === CHAPTER_END) return
        const c = color.get(id) ?? 0
        if (c === 1) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            path: ['chapters', ci],
            message: `第 ${ci + 1} 章 beat 图存在环（经过 "${id}"）`,
          })
          return
        }
        if (c === 2) return
        color.set(id, 1)
        for (const to of edges.get(id) ?? []) visit(to)
        color.set(id, 2)
      }
      visit(ch.entry)
      ch.beats.forEach((b) => {
        if (!color.has(b.id)) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            path: ['chapters', ci],
            message: `beat "${b.id}" 从 entry 不可达`,
          })
        }
      })
    })
    // 兜底结局：恰好最后一个无 when
    story.endings.forEach((e, ei) => {
      const isLast = ei === story.endings.length - 1
      if (isLast !== (e.when === undefined)) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          path: ['endings', ei],
          message: '兜底结局有且仅有一个：最后一项无 when、其余必须有 when',
        })
      }
    })
  })

export type Story = z.infer<typeof storySchema>
export type Chapter = Story['chapters'][number]
export type Beat = Chapter['beats'][number]
export type Choice = NonNullable<Beat['choices']>[number]
export type NextRef = NonNullable<Beat['next']>
export type Ending = Story['endings'][number]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- stories/schema`
Expected: PASS（7 tests）。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/stories/schema.ts src/stories/schema.test.ts
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(stories): 串珠故事 schema——章内 DAG、条件路由、兜底结局全量校验"
```

---

### Task 4: 引擎共用 fixture 与结局判定 `finale.ts`

**Files:**
- Create: `src/engine/fixtures.ts`, `src/engine/finale.ts`
- Test: `src/engine/finale.test.ts`

**Interfaces:**
- Consumes: `storySchema`、`checkCond`。
- Produces: `demoStory: Story`（两章小故事，供 reader/finale/storage 测试共用）；`resolveEnding(story: Story, flags: string[]): Ending`（顺序首个 `checkCond` 命中者，最后一项兜底）。

- [ ] **Step 1: 写 fixture**

`src/engine/fixtures.ts`：
```ts
import { storySchema, type Story } from '../stories/schema'

// 引擎测试共用的极小故事：两章、一个 flag、条件路由与双结局俱全。
export const demoStory: Story = storySchema.parse({
  id: 'demo',
  title: '示例小卷',
  tagline: '引擎测试用的两章小故事',
  chapters: [
    {
      id: 'c1',
      title: '岔路',
      entry: 'b1',
      beats: [
        {
          id: 'b1',
          narrative: '你站在岔路口。',
          choices: [
            { text: '向左', set: ['左'], to: 'b2' },
            { text: '向右', to: 'b2' },
          ],
        },
        { id: 'b2', narrative: '两条路在林中并拢。', next: [{ when: '左', to: 'b3' }, { to: '$end' }] },
        { id: 'b3', narrative: '左途多看见一片湖。', next: '$end' },
      ],
    },
    {
      id: 'c2',
      title: '归途',
      entry: 'b1',
      beats: [{ id: 'b1', narrative: '你踏上归途。', next: '$end' }],
    },
  ],
  endings: [
    { id: 'e-left', when: '左', title: '湖畔', epilogue: '你记得那片湖。' },
    { id: 'e-def', title: '归家', epilogue: '你回到了家。' },
  ],
})
```

- [ ] **Step 2: 写失败测试**

`src/engine/finale.test.ts`：
```ts
import { describe, expect, it } from 'vitest'
import { demoStory } from './fixtures'
import { resolveEnding } from './finale'

describe('resolveEnding', () => {
  it('命中带 when 的结局', () => {
    expect(resolveEnding(demoStory, ['左']).id).toBe('e-left')
  })
  it('无命中时落兜底', () => {
    expect(resolveEnding(demoStory, []).id).toBe('e-def')
  })
})
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- finale`
Expected: FAIL（finale.ts 不存在）。

- [ ] **Step 4: 最小实现**

`src/engine/finale.ts`：
```ts
import type { Ending, Story } from '../stories/schema'
import { checkCond } from './cond'

// 顺序首个条件命中者胜出；schema 保证最后一项无 when，必然兜底。
export function resolveEnding(story: Story, flags: string[]): Ending {
  return story.endings.find((e) => checkCond(e.when, flags))!
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- finale`
Expected: PASS（2 tests）。

- [ ] **Step 6: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/engine/fixtures.ts src/engine/finale.ts src/engine/finale.test.ts
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(engine): 结局判定与引擎测试 fixture"
```

---

### Task 5: 阅读引擎 `reader.ts`

**Files:**
- Create: `src/engine/types.ts`, `src/engine/reader.ts`
- Test: `src/engine/reader.test.ts`

**Interfaces:**
- Consumes: `Story/Beat/CHAPTER_END`、`checkCond`、`resolveEnding`、`demoStory`。
- Produces:
  - `interface LogEntry { chapter: number; beatId: string; choiceText?: string }`
  - `interface ReaderState { storyId: string; chapter: number; beatId: string | null; flags: string[]; log: LogEntry[]; ended?: { endingId: string } }`（`beatId === null` 表示停在章封面）
  - `startStory(story): ReaderState` / `enterChapter(story, st): ReaderState` / `currentBeat(story, st): Beat` / `advance(story, st): ReaderState` / `choose(story, st, i: number): ReaderState`

- [ ] **Step 1: 写失败测试**

`src/engine/reader.test.ts`：
```ts
import { describe, expect, it } from 'vitest'
import { demoStory } from './fixtures'
import { advance, choose, currentBeat, enterChapter, startStory } from './reader'

describe('reader', () => {
  it('startStory 停在第一章封面', () => {
    const st = startStory(demoStory)
    expect(st.chapter).toBe(0)
    expect(st.beatId).toBeNull()
  })
  it('enterChapter 进入 entry beat', () => {
    const st = enterChapter(demoStory, startStory(demoStory))
    expect(currentBeat(demoStory, st).id).toBe('b1')
  })
  it('choose 落 flag、记 log、按 to 跳转', () => {
    const st = choose(demoStory, enterChapter(demoStory, startStory(demoStory)), 0)
    expect(st.flags).toContain('左')
    expect(st.log.at(-1)).toEqual({ chapter: 0, beatId: 'b1', choiceText: '向左' })
    expect(st.beatId).toBe('b2')
  })
  it('advance 按 flag 解析条件 next', () => {
    let st = choose(demoStory, enterChapter(demoStory, startStory(demoStory)), 0)
    st = advance(demoStory, st) // b2 -[左]-> b3
    expect(st.beatId).toBe('b3')
  })
  it('无 flag 时条件 next 落兜底并跨章（回到章封面）', () => {
    let st = choose(demoStory, enterChapter(demoStory, startStory(demoStory)), 1)
    st = advance(demoStory, st) // b2 -> $end -> 第二章封面
    expect(st.chapter).toBe(1)
    expect(st.beatId).toBeNull()
    expect(st.ended).toBeUndefined()
  })
  it('末章走完判定结局', () => {
    let st = choose(demoStory, enterChapter(demoStory, startStory(demoStory)), 0)
    st = advance(demoStory, st) // -> b3
    st = advance(demoStory, st) // -> 第二章
    st = enterChapter(demoStory, st)
    st = advance(demoStory, st) // 末章 $end
    expect(st.ended?.endingId).toBe('e-left')
  })
  it('在选择节点调 advance 抛错；choose 越界抛错', () => {
    const st = enterChapter(demoStory, startStory(demoStory))
    expect(() => advance(demoStory, st)).toThrow()
    expect(() => choose(demoStory, st, 9)).toThrow()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- reader`
Expected: FAIL（reader.ts 不存在）。

- [ ] **Step 3: 实现**

`src/engine/types.ts`：
```ts
export interface LogEntry {
  chapter: number
  beatId: string
  choiceText?: string
}

// beatId === null：停在当前章的封面页（尚未 enterChapter）。
export interface ReaderState {
  storyId: string
  chapter: number
  beatId: string | null
  flags: string[]
  log: LogEntry[]
  ended?: { endingId: string }
}
```

`src/engine/reader.ts`：
```ts
import { CHAPTER_END, type Beat, type Story } from '../stories/schema'
import { checkCond } from './cond'
import { resolveEnding } from './finale'
import type { ReaderState } from './types'

export function startStory(story: Story): ReaderState {
  return { storyId: story.id, chapter: 0, beatId: null, flags: [], log: [] }
}

export function enterChapter(story: Story, st: ReaderState): ReaderState {
  return { ...st, beatId: story.chapters[st.chapter].entry }
}

export function currentBeat(story: Story, st: ReaderState): Beat {
  const ch = story.chapters[st.chapter]
  const beat = ch?.beats.find((b) => b.id === st.beatId)
  if (!beat) throw new Error(`当前位置无效：chapter=${st.chapter} beat=${st.beatId}`)
  return beat
}

function goto(story: Story, st: ReaderState, to: string): ReaderState {
  if (to !== CHAPTER_END) return { ...st, beatId: to }
  const next = st.chapter + 1
  if (next < story.chapters.length) return { ...st, chapter: next, beatId: null }
  return { ...st, beatId: null, ended: { endingId: resolveEnding(story, st.flags).id } }
}

export function advance(story: Story, st: ReaderState): ReaderState {
  const beat = currentBeat(story, st)
  if (beat.choices || beat.next === undefined) throw new Error(`beat "${beat.id}" 需要选择，不能直接推进`)
  const to =
    typeof beat.next === 'string' ? beat.next : beat.next.find((n) => checkCond(n.when, st.flags))!.to
  const log = [...st.log, { chapter: st.chapter, beatId: beat.id }]
  return goto(story, { ...st, log }, to)
}

export function choose(story: Story, st: ReaderState, index: number): ReaderState {
  const beat = currentBeat(story, st)
  const choice = beat.choices?.[index]
  if (!choice) throw new Error(`beat "${beat.id}" 无第 ${index} 个选项`)
  const flags = choice.set ? [...new Set([...st.flags, ...choice.set])] : st.flags
  const log = [...st.log, { chapter: st.chapter, beatId: beat.id, choiceText: choice.text }]
  return goto(story, { ...st, flags, log }, choice.to)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- reader`
Expected: PASS（7 tests）。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/engine/types.ts src/engine/reader.ts src/engine/reader.test.ts
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(engine): 阅读引擎——纯函数推进、flag 路由、跨章与结局收束"
```

---

### Task 6: 进度存续读 `storage.ts`

**Files:**
- Create: `src/engine/storage.ts`
- Test: `src/engine/storage.test.ts`

**Interfaces:**
- Consumes: `ReaderState`。
- Produces: `saveProgress(st: ReaderState): void` / `loadProgress(storyId: string): ReaderState | null` / `clearProgress(storyId: string): void`。键格式 `floating-life:save:<storyId>`，带版本号 `{ v: 1, st }`；损坏数据返回 null。

- [ ] **Step 1: 写失败测试**

`src/engine/storage.test.ts`（node 环境无 localStorage，用 Map 垫片）：
```ts
import { beforeEach, describe, expect, it } from 'vitest'
import type { ReaderState } from './types'
import { clearProgress, loadProgress, saveProgress } from './storage'

const store = new Map<string, string>()
beforeEach(() => {
  store.clear()
  globalThis.localStorage = {
    getItem: (k: string) => store.get(k) ?? null,
    setItem: (k: string, v: string) => void store.set(k, v),
    removeItem: (k: string) => void store.delete(k),
  } as Storage
})

const st: ReaderState = { storyId: 'demo', chapter: 1, beatId: 'b2', flags: ['左'], log: [] }

describe('storage', () => {
  it('存后可读回', () => {
    saveProgress(st)
    expect(loadProgress('demo')).toEqual(st)
  })
  it('无存档返回 null', () => {
    expect(loadProgress('nope')).toBeNull()
  })
  it('损坏 JSON 返回 null', () => {
    store.set('floating-life:save:demo', '{oops')
    expect(loadProgress('demo')).toBeNull()
  })
  it('clear 后读不到', () => {
    saveProgress(st)
    clearProgress('demo')
    expect(loadProgress('demo')).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- storage`
Expected: FAIL（storage.ts 不存在）。

- [ ] **Step 3: 最小实现**

`src/engine/storage.ts`：
```ts
import type { ReaderState } from './types'

const key = (storyId: string) => `floating-life:save:${storyId}`

export function saveProgress(st: ReaderState): void {
  localStorage.setItem(key(st.storyId), JSON.stringify({ v: 1, st }))
}

export function loadProgress(storyId: string): ReaderState | null {
  const raw = localStorage.getItem(key(storyId))
  if (!raw) return null
  try {
    const data = JSON.parse(raw) as { v: number; st: ReaderState }
    return data.v === 1 && data.st?.storyId === storyId ? data.st : null
  } catch {
    return null
  }
}

export function clearProgress(storyId: string): void {
  localStorage.removeItem(key(storyId))
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- storage`
Expected: PASS（4 tests）。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/engine/storage.ts src/engine/storage.test.ts
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(engine): localStorage 存续读——版本化、损坏容错"
```

---

### Task 7: App 状态机 + Home/Reader（demo 故事可玩）

**Files:**
- Create: `src/stories/index.ts`, `src/ui/Home.tsx`, `src/ui/Reader.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: 引擎全部 API、`demoStory`。
- Produces: `builtinStories: Story[]`；`<Home stories onStart(story) onContinue(story)>`；`<Reader story state onChoose(i) onAdvance()>`。App 内屏幕由状态推导：无 state→home；`ended`→ending（本任务先渲染简版）；`beatId===null`→cover（本任务先自动 enterChapter，Task 8 补真封面）。

- [ ] **Step 1: 故事注册表**

`src/stories/index.ts`（P1 先挂 demo，Task 13 换正式故事）：
```ts
import { demoStory } from '../engine/fixtures'
import type { Story } from './schema'

export const builtinStories: Story[] = [demoStory]
```

- [ ] **Step 2: 写 Home 与 Reader**

`src/ui/Home.tsx`：
```tsx
import type { Story } from '../stories/schema'
import { loadProgress } from '../engine/storage'

interface Props {
  stories: Story[]
  onStart: (story: Story) => void
  onContinue: (story: Story) => void
}

export default function Home({ stories, onStart, onContinue }: Props) {
  return (
    <main className="home">
      <h1 className="home-title">浮生长卷</h1>
      <p className="home-sub">编排式叙事 · 每一次选择都被长卷记得</p>
      {stories.map((s) => {
        const saved = loadProgress(s.id)
        return (
          <section key={s.id} className="story-card">
            <h2>{s.title}</h2>
            <p>{s.tagline}</p>
            <div className="story-actions">
              <button onClick={() => onStart(s)}>从头开卷</button>
              {saved && !saved.ended && <button onClick={() => onContinue(s)}>继续（第 {saved.chapter + 1} 章）</button>}
            </div>
          </section>
        )
      })}
    </main>
  )
}
```

`src/ui/Reader.tsx`：
```tsx
import type { Story } from '../stories/schema'
import type { ReaderState } from '../engine/types'
import { currentBeat } from '../engine/reader'

interface Props {
  story: Story
  state: ReaderState
  onChoose: (index: number) => void
  onAdvance: () => void
}

export default function Reader({ story, state, onChoose, onAdvance }: Props) {
  const chapter = story.chapters[state.chapter]
  const beat = currentBeat(story, state)
  return (
    <main className="reader">
      <header className="reader-chapter">
        第{state.chapter + 1}章 · {chapter.title}
      </header>
      <article className="reader-narrative">{beat.narrative}</article>
      {beat.choices ? (
        <div className="reader-choices">
          {beat.choices.map((c, i) => (
            <button key={i} onClick={() => onChoose(i)}>
              {c.text}
            </button>
          ))}
        </div>
      ) : (
        <button className="reader-continue" onClick={onAdvance}>
          继续 ▸
        </button>
      )}
    </main>
  )
}
```

- [ ] **Step 3: 重写 App 状态机**

`src/App.tsx`：
```tsx
import { useState } from 'react'
import { builtinStories } from './stories'
import type { Story } from './stories/schema'
import type { ReaderState } from './engine/types'
import { advance, choose, enterChapter, startStory } from './engine/reader'
import { clearProgress, loadProgress, saveProgress } from './engine/storage'
import Home from './ui/Home'
import Reader from './ui/Reader'

interface Session {
  story: Story
  state: ReaderState
}

export default function App() {
  const [session, setSession] = useState<Session | null>(null)

  const update = (story: Story, state: ReaderState) => {
    saveProgress(state)
    setSession({ story, state })
  }

  if (!session) {
    return (
      <Home
        stories={builtinStories}
        onStart={(story) => {
          clearProgress(story.id)
          update(story, startStory(story))
        }}
        onContinue={(story) => setSession({ story, state: loadProgress(story.id)! })}
      />
    )
  }

  const { story, state } = session

  if (state.ended) {
    const ending = story.endings.find((e) => e.id === state.ended!.endingId)!
    return (
      <main className="ending">
        <h2>{ending.title}</h2>
        <p>{ending.epilogue}</p>
        <button onClick={() => setSession(null)}>回到首页</button>
      </main>
    )
  }

  // 章封面：Task 8 换成真封面组件，这里先直接入章
  if (state.beatId === null) {
    update(story, enterChapter(story, state))
    return null
  }

  return (
    <Reader
      story={story}
      state={state}
      onChoose={(i) => update(story, choose(story, state, i))}
      onAdvance={() => update(story, advance(story, state))}
    />
  )
}
```

- [ ] **Step 4: 手工验证 demo 全流程**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" run dev`（后台）
浏览器打开 http://localhost:5173：从头开卷 → 「向左」→ 继续两次 → 第二章 → 继续 → 结局显示「湖畔」。刷新页面 → 首页出现「继续」。
Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test` 与 `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" run build`
Expected: 全部通过、构建成功。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add -A
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(ui): App 状态机与 Home/Reader——demo 故事全流程可玩"
```

---

### Task 8: 章封面 / 结局卡 / 回顾长卷 + 样式

**Files:**
- Create: `src/ui/ChapterCover.tsx`, `src/ui/EndingCard.tsx`, `src/ui/Recap.tsx`
- Modify: `src/App.tsx`, `src/styles.css`

**Interfaces:**
- Consumes: Task 7 全部。
- Produces: `<ChapterCover story state onEnter()>`（章号/题名/章引/史实注折叠）；`<EndingCard story ending log onRestart() onRecap()>`；`<Recap story log ending onBack()>`。

- [ ] **Step 1: 写三个组件**

`src/ui/ChapterCover.tsx`：
```tsx
import { useState } from 'react'
import type { Story } from '../stories/schema'
import type { ReaderState } from '../engine/types'

interface Props {
  story: Story
  state: ReaderState
  onEnter: () => void
}

export default function ChapterCover({ story, state, onEnter }: Props) {
  const ch = story.chapters[state.chapter]
  const [showNote, setShowNote] = useState(false)
  return (
    <main className="cover">
      <p className="cover-index">第 {state.chapter + 1} 章 · 共 {story.chapters.length} 章</p>
      <h2 className="cover-title">{ch.title}</h2>
      {ch.epigraph && <p className="cover-epigraph">{ch.epigraph}</p>}
      <button className="cover-enter" onClick={onEnter}>
        开卷 ▸
      </button>
      {ch.historyNote && (
        <div className="cover-note">
          <button className="cover-note-toggle" onClick={() => setShowNote((v) => !v)}>
            {showNote ? '收起史实注' : '史实注'}
          </button>
          {showNote && <p className="cover-note-body">{ch.historyNote}</p>}
        </div>
      )}
    </main>
  )
}
```

`src/ui/EndingCard.tsx`：
```tsx
import type { Ending, Story } from '../stories/schema'
import type { LogEntry } from '../engine/types'

interface Props {
  story: Story
  ending: Ending
  log: LogEntry[]
  onRestart: () => void
  onRecap: () => void
}

export default function EndingCard({ story, ending, log, onRestart, onRecap }: Props) {
  const choices = log.filter((l) => l.choiceText).length
  return (
    <main className="ending">
      <p className="ending-story">{story.title} · 终卷</p>
      <h2 className="ending-title">{ending.title}</h2>
      <p className="ending-epilogue">{ending.epilogue}</p>
      <p className="ending-meta">
        走过 {story.chapters.length} 章，做出 {choices} 次选择
      </p>
      <div className="ending-actions">
        <button onClick={onRecap}>回顾长卷</button>
        <button onClick={onRestart}>重开一卷</button>
      </div>
    </main>
  )
}
```

`src/ui/Recap.tsx`：
```tsx
import type { Ending, Story } from '../stories/schema'
import type { LogEntry } from '../engine/types'

interface Props {
  story: Story
  log: LogEntry[]
  ending: Ending
  onBack: () => void
}

// 回顾长卷：按章分组列出走过的节点与所做选择
export default function Recap({ story, log, ending, onBack }: Props) {
  return (
    <main className="recap">
      <h2>命运长卷 · {story.title}</h2>
      {story.chapters.map((ch, ci) => {
        const entries = log.filter((l) => l.chapter === ci)
        if (!entries.length) return null
        return (
          <section key={ch.id} className="recap-chapter">
            <h3>
              第{ci + 1}章 {ch.title}
            </h3>
            <ul>
              {entries
                .filter((e) => e.choiceText)
                .map((e, i) => (
                  <li key={i}>{e.choiceText}</li>
                ))}
            </ul>
          </section>
        )
      })}
      <p className="recap-ending">→ {ending.title}</p>
      <button onClick={onBack}>返回</button>
    </main>
  )
}
```

- [ ] **Step 2: 接入 App**

`src/App.tsx` 全量替换为：
```tsx
import { useState } from 'react'
import { builtinStories } from './stories'
import type { Story } from './stories/schema'
import type { ReaderState } from './engine/types'
import { advance, choose, enterChapter, startStory } from './engine/reader'
import { clearProgress, loadProgress, saveProgress } from './engine/storage'
import Home from './ui/Home'
import Reader from './ui/Reader'
import ChapterCover from './ui/ChapterCover'
import EndingCard from './ui/EndingCard'
import Recap from './ui/Recap'

interface Session {
  story: Story
  state: ReaderState
}

export default function App() {
  const [session, setSession] = useState<Session | null>(null)
  const [recap, setRecap] = useState(false)

  const update = (story: Story, state: ReaderState) => {
    saveProgress(state)
    setSession({ story, state })
  }

  if (!session) {
    return (
      <Home
        stories={builtinStories}
        onStart={(story) => {
          clearProgress(story.id)
          update(story, startStory(story))
        }}
        onContinue={(story) => setSession({ story, state: loadProgress(story.id)! })}
      />
    )
  }

  const { story, state } = session

  if (state.ended) {
    const ending = story.endings.find((e) => e.id === state.ended!.endingId)!
    if (recap) {
      return <Recap story={story} log={state.log} ending={ending} onBack={() => setRecap(false)} />
    }
    return (
      <EndingCard
        story={story}
        ending={ending}
        log={state.log}
        onRestart={() => {
          setRecap(false)
          setSession(null)
        }}
        onRecap={() => setRecap(true)}
      />
    )
  }

  if (state.beatId === null) {
    return <ChapterCover story={story} state={state} onEnter={() => update(story, enterChapter(story, state))} />
  }

  return (
    <Reader
      story={story}
      state={state}
      onChoose={(i) => update(story, choose(story, state, i))}
      onAdvance={() => update(story, advance(story, state))}
    />
  )
}
```

- [ ] **Step 3: 样式**

`src/styles.css` 全量替换为：
```css
:root {
  color-scheme: light;
  --paper: #f5f1e8;
  --ink: #2b2620;
  --dim: #7a6f5f;
  --accent: #8c3f2b;
}
* { box-sizing: border-box; }
body { margin: 0; font-family: 'Noto Serif SC', 'Songti SC', serif; background: var(--paper); color: var(--ink); }
main { max-width: 640px; margin: 0 auto; padding: 48px 24px; min-height: 100dvh; display: flex; flex-direction: column; }
button { font: inherit; cursor: pointer; border: 1px solid var(--ink); background: transparent; color: var(--ink); padding: 10px 20px; border-radius: 2px; }
button:hover { background: var(--ink); color: var(--paper); }

.home { align-items: center; justify-content: center; text-align: center; gap: 8px; }
.home-title { font-size: 2.4rem; letter-spacing: 0.3em; margin: 0; }
.home-sub { color: var(--dim); margin: 0 0 32px; }
.story-card { border: 1px solid #d8cfbe; padding: 24px 32px; width: 100%; }
.story-card h2 { margin: 0 0 8px; }
.story-card p { color: var(--dim); margin: 0 0 16px; }
.story-actions { display: flex; gap: 12px; justify-content: center; }

.cover { align-items: center; justify-content: center; text-align: center; gap: 16px; }
.cover-index { color: var(--dim); letter-spacing: 0.2em; }
.cover-title { font-size: 2rem; margin: 0; letter-spacing: 0.15em; }
.cover-epigraph { color: var(--dim); font-style: italic; max-width: 32em; }
.cover-enter { margin-top: 16px; }
.cover-note { margin-top: 24px; max-width: 36em; }
.cover-note-toggle { border: none; color: var(--accent); padding: 4px 8px; }
.cover-note-toggle:hover { background: transparent; color: var(--accent); text-decoration: underline; }
.cover-note-body { text-align: left; color: var(--dim); font-size: 0.9rem; line-height: 1.9; border-left: 2px solid var(--accent); padding-left: 12px; }

.reader { justify-content: center; gap: 24px; }
.reader-chapter { color: var(--dim); letter-spacing: 0.15em; font-size: 0.9rem; }
.reader-narrative { font-size: 1.05rem; line-height: 2.1; white-space: pre-wrap; }
.reader-choices { display: flex; flex-direction: column; gap: 12px; }
.reader-choices button, .reader-continue { text-align: left; padding: 14px 20px; }
.reader-continue { align-self: flex-end; border: none; color: var(--accent); }
.reader-continue:hover { background: transparent; color: var(--accent); text-decoration: underline; }

.ending { align-items: center; justify-content: center; text-align: center; gap: 12px; }
.ending-story { color: var(--dim); letter-spacing: 0.2em; }
.ending-title { font-size: 1.8rem; margin: 0; letter-spacing: 0.1em; }
.ending-epilogue { line-height: 2.1; max-width: 34em; text-align: left; }
.ending-meta { color: var(--dim); font-size: 0.9rem; }
.ending-actions { display: flex; gap: 12px; margin-top: 8px; }

.recap { gap: 8px; }
.recap-chapter h3 { margin: 16px 0 4px; font-size: 1rem; }
.recap-chapter ul { margin: 0; color: var(--dim); }
.recap-ending { color: var(--accent); margin-top: 16px; }
.recap button { align-self: flex-start; margin-top: 16px; }
```

- [ ] **Step 4: 手工验证 + 回归**

dev 服务器走一遍 demo 全流程：首页 → 章封面（开卷）→ 阅读/选择 → 第二章封面 → 结局卡 → 回顾长卷 → 重开。
Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test && npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" run build`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add -A
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(ui): 章封面/结局卡/回顾长卷与纸卷视觉——框架闭环完成"
```

---

## 《山河十四年》内容任务（Task 9–12）

**通用写作规约**（每个内容任务隐含遵守）：

- 主角：1931 年沈阳中学生，流亡入关，辗转成为报馆记者；第一人称贴身第三人称皆可，全书统一用第二人称「你」（与 015 一致）。
- 每章 5～6 个 beat，2～3 处选择；narrative 每 beat ≥100 字、文风克制有肌理（参考 015 spy 事件文案水准）；`historyNote` ≥80 字写明本章真实底本。
- **Step 1 永远是史实复核**：用 WebSearch 逐条核对该章锚点（日期、地名、名词），发现出入以检索结果为准修正细节；红线见 Global Constraints。
- 每章文件导出 `chNN: Chapter`（用 `Chapter` 类型标注，不单独 parse——装配时整卷 parse）。
- 全书 flag 总表（跨章回响的记忆）：

| flag | 落于 | 回响于 |
|---|---|---|
| 钢笔 / 相片 | 第1章（离家带走之物，二选一） | 第12章收拾行囊的条件分支 |
| 入行 | 第2章（帮报馆抄电讯） | 第3章编辑部支线措辞 |
| 上过街 | 第3章（一二九上街） | 第11章低谷回想 |
| 直笔 | 第5章（如实报道伤亡） | 结局「秉笔」条件 |
| 让渡 | 第6章（把船位让给母子） | 第12章归途一句回响 |
| 携孤 | 第6章（带上孤儿小满） | 第8章条件 beat、第12章、结局 |
| 守到最后 | 第7章（留守武汉发稿） | 第9章社论段落措辞 |
| 发稿 | 第10章（顶风发饥荒报道） | 结局「秉笔」条件 |
| 弃笔 | 第11章（投笔从戎） | 结局「投笔」条件 |

### Task 9: 第 1–3 章 + 故事装配

**Files:**
- Create: `src/stories/shanhe/ch01.ts`, `ch02.ts`, `ch03.ts`, `src/stories/shanhe/index.ts`
- Test: `src/stories/shanhe/shanhe.test.ts`

**Interfaces:**
- Produces: `shanhe: Story`（含临时结局表，Task 12 定稿）；integrity 测试骨架（后续任务只增章不改测试逻辑）。

- [ ] **Step 1: 史实复核（WebSearch）**

核对：九一八（1931.9.18 夜柳条湖爆炸、北大营；沈阳次日陷落）；一二八（1932.1.28 夜战起；商务印书馆 2.1 被炸、东方图书馆焚毁）；一二九（1935.12.9 北平学生游行，军警水龙大刀；背景为华北自治危机）。记录修正点。

- [ ] **Step 2: 写三章内容**

各章 beat 图（narrative 按此撰写，选择文案照抄）：

**ch01《城头的夜》1931 沈阳**（epigraph：那一夜之前，你还有一个完整的家）
- `b1` 前夜家常：父亲修钢笔、母亲缝衣，秋夜平常 → next `b2`
- `b2` 巨响与火光：choices「爬上房顶看北面（to b3）」「守着家人别出门（to b3）」（无 flag，措辞在 b3 汇合处呼应即可——两支共用 b3，narrative 写成两可）
- `b3` 破晓城变：街上是陌生的军队与告示 → next `b4`
- `b4` 收拾行装：choices「揣上父亲的钢笔（set 钢笔，to b5）」「揣上全家的相片（set 相片，to b5）」
- `b5` 车站离乡：南下的火车，回望城头 → next `$end`
- historyNote：柳条湖爆炸与北大营、沈阳一夜易手的时间线（复核后落笔）。

**ch02《纸灰落在租界》1932 上海**
- `b1` 亭子间寄居：投奔远房亲戚，白天在弄堂口帮工 → next `b2`
- `b2` 一二八夜：闸北方向炮响，火光映亮半个天 → next `b3`
- `b3` 纸灰如雪：东方图书馆焚毁，半城纸灰；choices「去报馆帮忙抄电讯（set 入行，to b4）」「去难民收容所帮工（to b4）」
- `b4` 停战后的街：春天来了，可街是旧的、人是少的 → next `b5`
- `b5` 一支笔的念头：你第一次想，有些事得有人写下来 → next `$end`

**ch03《北平的冬》1935**
- `b1` 古都求学：报馆分社打杂/旁听，华北的空气一年比一年紧 → next `b2`
- `b2` 集会前夜：同学递来明日游行的消息；choices「一起上街（set 上过街，to b3a）」「守在编辑部发稿（to b3b）」
- `b3a` 街上：水龙与大刀，你挨了一水柱，帮同学撕下横幅一角藏进怀里 → next `b4`
- `b3b` 编辑部：电话与电稿彻夜不停，你把街上的消息一条条发出去（若有 入行 flag，措辞多一句熟练）→ next `b4`
- `b4` 夜谈：屋檐下同伴问你，笔和石头哪个更有用；你没答，心里有了答案 → next `$end`

- [ ] **Step 3: 装配与 integrity 测试**

`src/stories/shanhe/index.ts`：
```ts
import { storySchema, type Story } from '../schema'
import { ch01 } from './ch01'
import { ch02 } from './ch02'
import { ch03 } from './ch03'

// 章节随任务推进逐个追加；结局表 Task 12 定稿，此前仅兜底。
export const shanhe: Story = storySchema.parse({
  id: 'shanhe',
  title: '山河十四年',
  tagline: '1931—1945，一个记者走过的中国',
  chapters: [ch01, ch02, ch03],
  endings: [{ id: 'e-home', title: '还乡', epilogue: '十四年后，你回到了出发的地方。' }],
})
```

`src/stories/shanhe/shanhe.test.ts`：
```ts
import { describe, expect, it } from 'vitest'
import { shanhe } from './index'

describe('山河十四年 内容完整性', () => {
  it('每章 historyNote 必填且 ≥80 字', () => {
    for (const ch of shanhe.chapters) {
      expect(ch.historyNote, `${ch.title} 缺史实注`).toBeTruthy()
      expect(ch.historyNote!.length, `${ch.title} 史实注过短`).toBeGreaterThanOrEqual(80)
    }
  })
  it('每个 beat narrative ≥100 字', () => {
    for (const ch of shanhe.chapters)
      for (const b of ch.beats)
        expect(b.narrative.length, `${ch.title}/${b.id} 文案过短`).toBeGreaterThanOrEqual(100)
  })
  it('每章至少一处选择', () => {
    for (const ch of shanhe.chapters) expect(ch.beats.some((b) => b.choices)).toBe(true)
  })
})
```

（schema 的 DAG/可达性/引用校验在 `storySchema.parse` 时自动生效——parse 抛错即测试失败。）

- [ ] **Step 4: 跑测试**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- shanhe`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/stories/shanhe
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(shanhe): 第1-3章——九一八离乡、一二八纸灰、一二九冬"
```

### Task 10: 第 4–6 章

**Files:**
- Create: `src/stories/shanhe/ch04.ts`, `ch05.ts`, `ch06.ts`
- Modify: `src/stories/shanhe/index.ts`（chapters 数组追加 ch04–ch06）

- [ ] **Step 1: 史实复核（WebSearch）**

核对：七七（1937.7.7 卢沟桥；北平 7 月底陷落）；淞沪（1937.8.13 开战；四行仓库 10 月末，苏州河南岸市民围观）；南京（11 月中旬撤退潮起，12.13 陷落；下关江边渡口拥挤史实）。**第 6 章红线复审：暴行只经幸存者转述呈现，主角随难民西撤、不入城。**

- [ ] **Step 2: 写三章**

**ch04《桥上的枪声》1937 北平**
- `b1` 记者证：你已是正式记者，七月的北平闷热而绷紧 → next `b2`
- `b2` 卢沟桥的消息：凌晨电讯，宛平城外枪声 → next `b3`
- `b3` 去留：choices「随报社南迁（to b4）」「留几日，把北平的最后模样写完（to b4）」（两支在 b4 汇合，narrative 两可）
- `b4` 出城：卡口、盘查、七月末的告示——古都换了旗 → next `b5`
- `b5` 南下车厢：满车都是不肯做亡国奴的人 → next `$end`

**ch05《苏州河的秋天》1937 上海**
- `b1` 战地记者：八一三开战，你领到战地记者臂章 → next `b2`
- `b2` 前线见闻：罗店方向，担架队与新兵的脸 → next `b3`
- `b3` 发稿抉择：choices「如实写伤亡与撤退（set 直笔，to b4）」「写成鼓舞士气的檄文（to b4）」
- `b4` 河畔望仓库：十月末，苏州河南岸人群望北岸孤楼 → next `b5`
- `b5` 西撤令：报馆西迁，你收拾稿纸 → next `$end`

**ch06《江水向西》1937 冬**
- `b1` 撤离令：南京不可久留，报馆分批西撤 → next `b2`
- `b2` 江边渡口：一票难求；choices「把舱位让给带孩子的母亲，自己等下一班（set 让渡，to b3）」「按证登船（to b3）」
- `b3` 孤儿小满：码头上拽住你衣角的孩子，家人失散；choices「带上他（set 携孤，to b4）」「托付给难民救济的先生（to b4）」
- `b4` 溯江而上：船过芜湖、九江，冬雨连绵 → next `b5`
- `b5` 武汉码头：你听一位下关来的幸存者低声讲完，一夜没写出一个字——有些事，笔要慢慢才敢碰 → next `$end`

- [ ] **Step 3: 跑测试（integrity 自动覆盖新章）**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- shanhe`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/stories/shanhe
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(shanhe): 第4-6章——七七出城、淞沪直笔、江水向西"
```

### Task 11: 第 7–9 章

**Files:**
- Create: `src/stories/shanhe/ch07.ts`, `ch08.ts`, `ch09.ts`
- Modify: `src/stories/shanhe/index.ts`（追加）

- [ ] **Step 1: 史实复核（WebSearch）**

核对：台儿庄（1938.4 捷报）与武汉撤退（1938.10 弃守、大撤退）；重庆大轰炸（1939 五三五四为烈，防空洞、雾季喘息）；珍珠港与日军进占上海租界（1941.12.8，孤岛终结）。

- [ ] **Step 2: 写三章**

**ch07《大江东去人西行》1938 武汉**
- `b1` 春天的捷报：台儿庄的号外，街头的爆竹 → next `b2`
- `b2` 伤兵医院：你采访一个再也回不了前线的兵 → next `b3`
- `b3` 秋撤退：choices「随难民船先行入川（to b4）」「留到最后一批，把撤退写完（set 守到最后，to b4）」
- `b4` 江上回望：武汉的灯火退成一线 → next `$end`
（本章 4 beat 即可，选择一处。）

**ch08《雾都洞中》1939–40 重庆**
- `b1` 雾季与警报：山城的日常被警报切成两半 → next `b2`
- `b2` 洞口：choices「把位置让给邻家老人，自己躲在岩檐下（set 让位，to b3）」「挤进洞里（to b3）」
- `b3` 余烬中出报：印厂中弹，报纸改出半张，照出 → next `[{ when: '携孤', to: 'b4' }, { to: '$end' }]`
- `b4`（携孤限定）小满上学了：他在防空洞里都攥着课本；你想起江边那一拽 → next `$end`

**ch09《电讯房的冬夜》1941 重庆**
- `b1` 十二月八日：电讯房彻夜响，太平洋开战 → next `b2`
- `b2` 上海的最后一封信：孤岛旧识来信说租界完了，此后再无音讯；choices「连夜写社论《暗夜与黎明》（to b3）」「把信收进抽屉，先把手头的版做完（to b3）」（若有 守到最后 flag，b3 措辞多一句「你见过撤退，知道等待是什么」）
- `b3` 同盟的消息：战争成了世界的战争，冬天依旧很长 → next `$end`

- [ ] **Step 3: 跑测试**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- shanhe`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/stories/shanhe
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(shanhe): 第7-9章——武汉撤退、雾都洞中、孤岛终结的冬夜"
```

### Task 12: 第 10–12 章 + 结局定稿

**Files:**
- Create: `src/stories/shanhe/ch10.ts`, `ch11.ts`, `ch12.ts`
- Modify: `src/stories/shanhe/index.ts`（追加三章 + 结局表定稿）
- Modify: `src/stories/shanhe/shanhe.test.ts`（补终局断言）

- [ ] **Step 1: 史实复核（WebSearch）**

核对：河南 1942 大饥荒与报道封锁（大公报 1943.2《豫灾实录》后停刊三日、白修德报道）；豫湘桂（1944 溃退、湘桂大撤退、12 月独山止步）；胜利（1945.8.10 夜重庆沸腾、8.15 诏书、还乡潮）。**第 10 章红线复审：灾情克制侧写。**

- [ ] **Step 2: 写三章**

**ch10《中原的尘土》1942–43**
- `b1` 入豫：你随赈济的卡车进入灾区，路比传闻更长 → next `b2`
- `b2` 所见：克制的白描——空了的村、树皮、一口没人应的井；你把每一笔都记实，不形容 → next `b3`
- `b3` 发稿抉择：choices「顶着停刊的风险发出去（set 发稿，to b4a）」「存档，等能发的那天（to b4b）」
- `b4a` 后果：报纸开了天窗又停刊数日，你被记了处分；深夜有同行敬你一杯 → next `$end`
- `b4b` 压箱：稿纸压进箱底，你在扉页写：欠中原一笔 → next `$end`

**ch11《最长的下坡》1944**
- `b1` 溃退的秋天：湘桂路上，撤退的人流没有尽头 → next `b2`
- `b2` 笔的意义：你的报道追不上溃退的速度；choices「投笔从戎（set 弃笔，to b3a）」「继续写，哪怕没人来得及读（to b3b）」（若有 上过街 flag，选项前 narrative 多一句「你想起九年前街上的水龙」）
- `b3a` 军中一冬：战线止在黔南，你在工事里过了年 → next `b4`
- `b3b` 独山道上：你在撤退队尾记下每一个地名，像立碑 → next `b4`
- `b4` 除夕微光：有人在废墟上贴了春联 → next `$end`

**ch12《还乡》1945**
- `b1` 那一夜：八月十日夜，重庆满城鞭炮与哭笑 → next `b2`
- `b2` 收拾行囊：→ next `[{ when: '钢笔', to: 'b2a' }, { to: 'b2b' }]`
- `b2a` 钢笔：父亲的钢笔写秃了三个尖，十四年没离身 → next `b3`
- `b2b` 相片：相片褪了色，你对着它认了很久——你比相片里的父亲还老了 → next `b3`
- `b3` 归途：→ next `[{ when: '携孤', to: 'b3a' }, { to: 'b4' }]`
- `b3a` 带小满回家：他已比你高半头，管你叫先生，也叫过一次爹 → next `b4`
- `b4` 沈阳城下：火车过山海关，你在城头认出了那年的月台（若有 让渡 flag，加一句：你又想起江边那位母亲，愿她们也在还乡的路上）→ next `$end`

- [ ] **Step 3: 结局表定稿**

`src/stories/shanhe/index.ts` 的 endings 替换为：
```ts
endings: [
  {
    id: 'e-pen',
    when: '直笔 & 发稿 & !弃笔',
    title: '秉笔十四年',
    epilogue:
      '战后清点旧稿，从苏州河到中原，你没有写过一个自己不信的字。有人问你怕过没有，你说：怕过，每一次都怕。可十四年了，笔比你勇敢。',
  },
  {
    id: 'e-child',
    when: '携孤',
    title: '带小满回家',
    epilogue:
      '沈阳的雪落下来的时候，小满站在你家老宅的门槛上，替你念出了门楣上剩下的半块匾。你忽然明白，十四年里你带回来的不是一个孩子，是一个可以重新开始的理由。',
  },
  {
    id: 'e-sword',
    when: '弃笔',
    title: '投笔之后',
    epilogue:
      '你把最后一篇没写完的稿子留在了黔南的工事里。胜利那天你摸了摸口袋，只有一枚弹壳。有人替你把那篇稿子登了出来，署名是：一个不愿留名的士兵。',
  },
  {
    id: 'e-home',
    title: '还乡',
    epilogue:
      '十四年，三千里，你回到了出发的月台。站牌换过三次，月台还是那个月台。你放下行李，把这十四年从头想了一遍——然后走出站去，像每一个普通的还乡人。',
  },
],
```

- [ ] **Step 4: 补终局断言**

`shanhe.test.ts` 追加：
```ts
import { resolveEnding } from '../../engine/finale'

it('全书 12 章', () => {
  expect(shanhe.chapters.length).toBe(12)
})
it('四结局均可达', () => {
  expect(resolveEnding(shanhe, ['直笔', '发稿']).id).toBe('e-pen')
  expect(resolveEnding(shanhe, ['携孤']).id).toBe('e-child')
  expect(resolveEnding(shanhe, ['弃笔']).id).toBe('e-sword')
  expect(resolveEnding(shanhe, []).id).toBe('e-home')
})
it('秉笔优先于携孤（顺序即优先级）', () => {
  expect(resolveEnding(shanhe, ['直笔', '发稿', '携孤']).id).toBe('e-pen')
})
```

- [ ] **Step 5: 跑测试**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test -- shanhe`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add src/stories/shanhe
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat(shanhe): 第10-12章与四结局——中原、低谷、还乡"
```

---

### Task 13: 正式接入首页 + README + 验收

**Files:**
- Modify: `src/stories/index.ts`
- Create: `README.md`

- [ ] **Step 1: 首页换正式故事**

`src/stories/index.ts` 全量替换：
```ts
import type { Story } from './schema'
import { shanhe } from './shanhe'

export const builtinStories: Story[] = [shanhe]
```
（demo 故事保留在 `engine/fixtures.ts` 仅供测试——首页不再展示。）

- [ ] **Step 2: README**

`README.md`：
```markdown
# 浮生长卷 · floating-life

> 365 开源计划 #021 · 编排式叙事游戏框架 — 作者编排故事，引擎负责推进与记忆

千世书（#015）的镜像姐妹作：千世书用数值涌现故事，浮生长卷用编排承载故事。
故事是数据：线性章节串（珠）、章内分支章末汇聚（DAG）、选择只落 flag、flag 驱动回响与结局。

首发故事 **《山河十四年》**：1931 九一八 → 1945 胜利，一个记者走过的中国。
十二章，每章附史实注；零配置、无需 API Key，打开即读。

## 快速开始

​```bash
npm install
npm run dev   # http://localhost:5173
npm test
npm run build # 纯静态，dist/ 可部署到任意静态托管
​```

## 故事格式

见 `src/stories/schema.ts`（zod 定义即文档）：Story → chapters[]（章）→ beats[]（节点 DAG）
→ choices（落 flag）→ endings（按 flag 分化，末项兜底）。
```

- [ ] **Step 3: 全量验收**

Run: `npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" test && npm --prefix "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" run build`
Expected: 全部测试通过、构建成功。
dev 服务器手工通读《山河十四年》至少一条完整路线（建议：直笔+发稿线走到「秉笔十四年」），检查：章封面/史实注/选择回响/结局卡/回顾长卷/续读。

- [ ] **Step 4: Commit**

```bash
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" add -A
git -C "D:\Backup\Libraries\Documents\GitHub\Projects\365\021" commit -m "feat: 《山河十四年》正式接入首页，P1 完整可玩"
```

---

## Self-Review 记录

- **Spec 覆盖**：串珠模型（T3）、引擎推进/flag/结局（T4–5）、存续读（T6）、视觉小说 UI 与回顾长卷（T7–8）、《山河十四年》12 章＋史实注＋克制红线（T9–12）、多故事注册表（T7/T13）。спec 中 P2（AI 润色层）不在本计划范围。art 字段已建模、P1 不产图（符合 spec"配图走管线"留待后续）。
- **占位扫描**：内容任务的 narrative 以 beat 图＋文案要点交付（创作在执行时完成并受 integrity 测试的长度/结构约束），选择文案、flag、路由、结局文案均已写死——无 TBD。
- **类型一致性**：`checkCond/resolveEnding/startStory/enterChapter/currentBeat/advance/choose/saveProgress/loadProgress/clearProgress` 的签名在各任务间已核对一致；`CHAPTER_END` 统一从 schema 导出。
```
